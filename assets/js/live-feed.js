// Tagtaly Live Feed Module
// Handles real-time article updates and live feed functionality

console.log('Live feed module loaded');

// Initialize live feed when DOM is ready
document.addEventListener('DOMContentLoaded', function() {
    console.log('Live feed initialized');
});
